create table if not exists conversations (id bigint generated by default as identity primary key, created_at timestamptz not null default now(), phone text, call_id text, user_text text, gemini_text text);\ncreate index if not exists conversations_phone_created_idx on conversations(phone, created_at desc);\n